
- Add a register link to the login screen + add a new view/controller for it. 
	(What about a password reset?)
	(What about a logout feature?)
	(Once logged in, does the user ever 'expire'?)
	
- When saving entering a new log, store the numeric data as a custom attribute of the log label for easy retrieval
- When closing the log entry view, save the step log to the newly created log local DB model

- Add 'sync' support